# Web (Vite + React)

## Setup
1) Create env
- `copy .env.example .env.local`
- Set `VITE_API_KEY` to match `apps/api/.env` API_KEY

## Run
- from repo root: `pnpm -r install`
- `pnpm --filter @scod/web dev`

Open: http://127.0.0.1:5173
